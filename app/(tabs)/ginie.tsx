import { View, Text } from "react-native";

export default function GinieScreen() {
  return (
    <View
      style={{
        flex: 1,
        backgroundColor: "#000",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text style={{ color: "white" }}>Ginie Screen</Text>
    </View>
  );
}
